{{#if view.canCreate}}
  <h3>Create {{view.title}}</h3>
  <form-widget {form-object}="getNewObject()" show-saving (submit)="saveObject" {fields}="_fields" />
{{else}}
  <p>{{view.addDisabledMessage}}</p>
{{/if}}
